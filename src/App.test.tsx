import React from 'react';
import {render, screen} from '@testing-library/react';
import App from './App';

describe('Test for App Component', () => {
  test('should render App peacefully', () => {});

  test('should output App Component inner markup structure', () => {
    // screen.debug();
  });
});
